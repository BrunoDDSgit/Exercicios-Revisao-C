#include <stdio.h>
#include <stdlib.h>

int main() {
    /*

    A) A diferença entre pilha e fila é que:
    Pilha: O primeiro elemento a sair é o ultimo que entrou, tanto na inserção(push) quanto na remoção(pop) 
    atuamos em um único extremo(Topo) Ou seja, Segue o princípio LIFO - Last In First Out.
    Fila: O primeiro elemento a sair é o primeiro que entrou, então atuamos em 2 extremidades diferentes na
    inserção(Cauda) e remoção de elementos(Cabeça) de uma fila, sendo o contrário da pilha e seguindo o princípio FIFO - First In e First Out.
    
    B) FILA:
    Vantagens: Preserva estritamente a ordem de chegada dos dados e é 
    ideal para gerenciamento de recursos compartilhados ou assíncronos.
    Desvantagens: O acesso aos dados é limitado apenas às extremidades (não permite busca 
    ou remoção direta no meio da fila sem desenfileirar os elementos anteriores).

    C) PILHA:
    Vantagens: Extremamente simples de implementar e eficiente em memória. Excelente para rastrear estados passados, útil para
    gerenciar chamadas de funções e inverter sequências.
    Desvantagens: Assim como a fila, possui acesso restrito -> apenas o elemento do topo está acessível por vez.

    D)
    Pilha:
    Histórico de navegação web (botão "Voltar").
    Recurso de desfazer/refazer (Ctrl+Z) em editores.
    Chamadas de funções e recursão no compilador/interpretador.
    Avaliação de expressões matemáticas (notação pós-fixa).

    Fila:
    Fila de impressão de documentos.
    Processamento de tarefas em sistemas operacionais.
    Buffers de streaming de vídeo/áudio.
    Filas de mensagens.

    E)
    Acesso e Inserção: Enquanto a pilha e a fila possuem regras restritas de inserção e remoção (apenas nas extremidades), 
    a Lista permite acesso, inserção e remoção em qualquer posição (início, meio ou fim).

    Uso Geral: A lista é uma estrutura de propósito geral; a pilha e a fila são estruturas especializadas para 
    padrões específicos de acesso aos dados.
    */

}